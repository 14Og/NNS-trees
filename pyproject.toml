[project]
name = "nns-trees"
version = "0.1.0"
description = "Nearest Neighbour Search data structures comparison (QuadTree vs KDTree)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.9",
    "numpy>=2.4.6",
]